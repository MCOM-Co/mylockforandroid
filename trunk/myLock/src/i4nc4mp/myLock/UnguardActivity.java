package i4nc4mp.myLock;

import android.app.Activity;

public class UnguardActivity extends Activity {
	//guard screen that's already dismissed keyguard at create
	//allowing all keys to wake
	//we've programmed it to autoexit if it has focus at resume
}